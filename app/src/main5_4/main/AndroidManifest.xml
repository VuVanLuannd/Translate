<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.translatedemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".ui.MyApplication"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
        </activity>
        <!-- <activity android:name=".fragment.FavoritesInforFragment" /> -->
        <activity android:name=".fragment.InfomationFragment" />
        <activity android:name=".ui.NavigationInfWebview" />
        <activity android:name=".ui.NavigationInformation" />
        <activity android:name=".ui.Main2Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ui.FloatViewServeceActivity"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>